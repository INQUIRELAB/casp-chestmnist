# --- Reproducible-run dependencies (exact pins; these determine the reported
#     numbers and figures via reproduce_paper.py / plots.py / ece.py / nih_bootstrap.py) ---
numpy==2.3.1
scikit-learn==1.7.2
matplotlib==3.10.8
# --- Training + FLOPs dependencies (needed only to RETRAIN or recompute MACs,
#     not for the reproducible run above) ---
torch>=2.1
torchvision>=0.16
medmnist>=3.0
fvcore>=0.1.5
